#include <stdio.h>
#include <math.h>

int main()
{

    extern int dob[3];
     extern int  calc_year;
    {
        printf("Hello, Please enter you DOB: ");
        scanf("%d%d%d", &dob);
    }

    {
        static int countYear = 0;
        if(dob[2]: dob[2]==2023;dob[2] = dob[2]++)
            while (dob[2]<2023)
            {
                countYear++;
            }
            calc_year = countYear;

    }

    return 0;
}